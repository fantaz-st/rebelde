/* import { useVideoTexture } from "@react-three/drei";

const useVideoTextures = (slides) => {
 const textures = [];

  for (let i = 0; i < slides.length; i++) {
    const texture = useVideoTexture(slides[i].src);
    textures.push(texture);
  }
  return textures;
};

export default useVideoTextures;
 */
